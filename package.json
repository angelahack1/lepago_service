{
  "name": "lepago_service1",
  "version": "1.0.0",
  "main": "index.cjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.cjs",
    "dev": "nodemon index.cjs",
    "build": "tsc",
    "clean_windows": "(if exist node_modules rd /s /q node_modules) && (if exist .next rd /s /q .next) && (if exist out rd /s /q out) && (if exist package-lock.json rd /s /q package-lock.json)",
    "clean_linux": "rm -rf node_modules && rm -rf .next && rm -rf out && rm -rf package-lock.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.5.0",
    "express": "^5.1.0",
    "http": "^0.0.1-security",
    "lepago_service": "file:",
    "mlkem": "^2.3.1",
    "mongodb": "^6.16.0",
    "qrcode": "^1.5.4",
    "soap": "^1.1.11",
    "fs": "^0.0.1-security"
  },
  "devDependencies": {
    "@types/node": "^22.15.17",
    "nodemon": "^3.1.10",
    "typescript": "^5.8.3"
  },
  "description": ""
}
